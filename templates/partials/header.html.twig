<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{path('homepage')}}">SymBNB </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="# navbarColor02"
        aria-controls="navbarColor02" aria-extended="false" aria-label="Basculer la navigation ">
        <span class="navbar-toggler-icon"> </span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor02">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item ">
                <a class="nav-link" href="{{path('homepage')}}"> Accueil</a>
            </li>
            <li class="nav-item ">
                <a class="nav-link" href="{{path('ads_index')}}"> Annonces</a>
            </li>


        </ul>
        <Ul class="navbar-nav ml-auto">
            <li class="navbar-item">
                <a href="#" class="nav-link">Mon compte</a>
            </li>
        </Ul>
    </div>
</nav>

{% for label, message in app.flashes %}
<div class="contaire">
    <div class="alert alert-{{label}}">
        {% for message in message %}
        <p>{{message | raw }}</p>
        {% endfor %}
    </div>
</div>

{% endfor %}